//
//  Muscledatamenu.swift
//  Ren-Swiftui
//
//  Created by betterchris on 2022/4/7.
//

import SwiftUI

/*
 
 struct Muscledatamenu_Previews: PreviewProvider {
     static var previews: some View {
         DisplayedView {
             Muscledatamenu(pagedto: .constant(.exercisedata), menu: .exercisedata)
         }
     }
 }
 
 
 struct Muscledatamenu: View {

     var menu: Activitymuscledatatype

     var focused: Bool {
         menu == pagedto
     }

     var body: some View {
         VStack {
             LocaleText(menu.rawValue)
                 .font(.system(size: DEFINE_FONT_SMALL_SIZE - 1).bold())
                 .foregroundColor(
                     focused ? NORMAL_LIGHTER_COLOR : NORMAL_GRAY_COLOR
                 )
         }
         .frame(width: 80)
         .contentShape(Rectangle())
         .onTapGesture {
             pagedto = menu
         }
     }
 }

 
 */
